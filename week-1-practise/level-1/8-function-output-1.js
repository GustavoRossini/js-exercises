function getNumber(a) {
    return Math.random(a) * 10;
}

var result = getNumber(2);
console.log(result);

// Answer these questions:
// - What does this program do? Mathematical Party
// - How many parameters does the function take? One
// - What is the function name? getNumber
// - Where is the function called? with what parameters? var result = getNumber (2)